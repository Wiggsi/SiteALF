{% extends 'base.html.twig' %}

{% block title %}{{ criminel }} | Modification{% endblock %}
{% block pageTitle %}Traitement d'antécédents judiciaires{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h3>{{ criminel }} - Modification</h3>
            <div class="btn-group p-1">
                <a href="{{ path('gd_taj_show', {'id': criminel.id}) }}" class="btn btn-outline-secondary">
                    <span class="oi oi-chevron-left"></span> Retour à la fiche</a>
                {% if app.user.gendarme.isUnit('SR') %}
                    {{ include('Crim/_delete_form.html.twig') }}
                {% endif %}
            </div>
        </div>
    </div>
    {{ include('Crim/_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/summernote/0.8.9/summernote-bs4.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/summernote/0.8.9/summernote-bs4.js"></script>7
    {#<script src="{{ asset('js/rangeslider.min.js') }}"></script>#}
    {#<script src="{{ asset('css/rangeslider.css') }}"></script>#}
    <script>
        let bb = document.querySelector('#colContent').getBoundingClientRect();
        width = bb.right - bb.left;
        $(document).ready(function () {
            $('#criminel_commentaires').summernote({
                height: screen.availHeight * 0.5,
                width: width * 0.9
            });
            /*            $('#criminel_dangerous').rangeslider({
                            polyfill: false,
                            onInit: function () {
                                this.output = $('<div class="range-output" />').insertAfter(this.$range).html(this.$element.val());
                            },
                            onSlide: function (position, value) {
                                this.output.html(value);
                            }
                        });*/
            // $('input[type="range"]').rangeslider('destroy');
            // $('input[type="range"]').rangeslider('update', true);
        });
    </script>
{% endblock %}