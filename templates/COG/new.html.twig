{% extends 'base.html.twig' %}

{% block title %}Nouvel Appel | COG{% endblock %}
{% block pageTitle %}Centre d'Opérations de la Gendarmerie{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h3>Nouvel appel
                <a href="{{ path('gd_cog') }}" class="btn btn-outline-secondary btn-sm">
                    <span class="oi oi-list"></span> Retour au COG</a></h3>
        </div>
    </div>
    {{ include('COG/_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/summernote/0.8.9/summernote-bs4.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/summernote/0.8.9/summernote-bs4.js"></script>
    <script>
        let bb = document.querySelector('#colContent').getBoundingClientRect();
        width = bb.right - bb.left;
        $(document).ready(function () {
            $('#appel_cog_content').summernote({
                height: screen.availHeight * 0.5,
                width: width * 0.9
            });
        });
    </script>
{% endblock %}